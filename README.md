# an API system that contains AUTH , EXPENSE model and INCOME Model

##  Installation Process
```xml
pipenv shell
```

```xml
pipenv install django djangorestframework  drf-yasg gunicorn django-heroku dj-database-url whitenoise 
```